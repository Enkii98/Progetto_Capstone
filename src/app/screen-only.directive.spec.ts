import { ScreenOnlyDirective } from './screen-only.directive';

describe('ScreenOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreenOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
